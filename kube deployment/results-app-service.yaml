apiVersion: v1
kind: Service
metadata:
  name: results-app
spec:
  type: NodePort
  selector:
    app: results-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
